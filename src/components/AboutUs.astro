---
import { Button } from "./ui/button";
---

<div class="flex flex-col overflow-hidden items-center text-white relative">
  <img
    class="bg-cover hidden lg:block min-w-[110vw] hue-rotate-[70deg] saturate-200 bg-[#000] h-full absolute -z-10"
    src="/images/bg.png"
  />
  <img
    class="bg-cover lg:hidden min-w-[110vw] hue-rotate-[70deg] saturate-200 bg-[#000] h-full absolute -z-10"
    src="/images/bgmobile.png"
  />

  <div class="flex flex-col lg:pt-72 pt-32 lg:pb-32 pb-16 lg:gap-32 gap-16">
    <div
      class="lg:px-10 px-5 lg:flex-row flex flex-col items-center lg:gap-[100px] gap-6"
    >
      <img
        style={{ opacity: 0 }}
        data-firstpicmob
        class="lg:hidden h-80"
        src="/images/aboutusimg1.svg"
      />

      <div class="flex flex-col lg:gap-10 gap-6">
        <div class="flex flex-col lg:gap-6 gap-[18px]">
          <h1
            class="lg:text-5xl font-BoldItalic text-2xl italic font-black uppercase"
          >
            Content is <span class="text-[#a2b6fd]">91%</span> of <br
              class="hidden lg:block"
            />
            global internet traffic. <span class="text-[#a2b6fd]"
              >Drake makes it actionable</span
            >
          </h1>

          <p class="lg:text-xl font-light">
            We enable businesses to extract insights and take action at the same <br
              class="hidden lg:block"
            /> speed and scale that content is created and consumed. Our AI Agents
            <br class="hidden lg:block" /> reduce the cost of real-time data extraction
            by up to 90%, while
            <br class="hidden lg:block" /> collecting 10x the number of inferences.
          </p>
        </div>

        <Button>Start Building with drake</Button>
      </div>

      <img
        style={{ opacity: 0 }}
        data-firstpic
        class="hidden lg:block"
        src="/images/aboutusimg1.svg"
      />
    </div>

    <div
      class="lg:px-10 px-5 lg:flex-row flex flex-col items-center lg:gap-[100px] gap-6"
    >
      <img
        style={{ opacity: 0 }}
        data-secondpic
        class="lg:min-w-[600px] lg:h-[500px] h-52 hue-rotate-[75deg]"
        src="/images/aboutusimg2.svg"
      />

      <div class="flex flex-col lg:gap-10 gap-6">
        <div class="flex flex-col lg:gap-6 gap-[18px]">
          <h1
            class="lg:text-5xl font-BoldItalic text-2xl tracking-tighter font-black italic uppercase"
          >
            Drake studio changes the <br /> game for decentralized <br /> ai vision
          </h1>

          <p class="lg:text-xl font-light">
            You don’t need to be a programmer or data scientist to train
            powerful AI models to extract data. Our easy to use platform enables
            non-technical users to deploy AI agents optimized for social,
            streaming, gaming, <br /> browsing and sports content in just a few minutes.
          </p>
        </div>

        <Button>Access the Beta</Button>
      </div>
    </div>
  </div>
</div>

<style>
  .fadeInScale {
    opacity: 0;
    transform: scale(0.7);
    animation: fadeInScale 1s forwards; /* 1s duration, forwards to hold final state */
    animation-delay: 0.25s; /* same 0.5s delay you had before */
  }

  @keyframes fadeInScale {
    to {
      opacity: 1;
      transform: scale(1);
    }
  }
</style>

<script>
  import { inView } from "motion";

  // Check media query
  const mql = window.matchMedia("(width <= 640px)");

  if (mql.matches) {
    inView("[data-firstpicmob]", (element) => {
      // Instead of animate(), just add a CSS class:
      element.classList.add("fadeInScale");
    });
  } else {
    inView("[data-firstpic]", (element) => {
      element.classList.add("fadeInScale");
    });
  }

  inView("[data-secondpic]", (element) => {
    element.classList.add("fadeInScale");
  });
</script>
